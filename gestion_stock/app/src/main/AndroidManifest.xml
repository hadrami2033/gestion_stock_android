<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestionstock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.Home" />
        <activity android:name=".app.Produit" />
        <activity android:name=".app.Client" />
        <activity android:name=".app.Fournisseur" />
        <activity android:name=".app.OpperationAchat" />
        <activity android:name=".app.OpperationVente" />
        <activity android:name=".app.ProduitDetail" />
        <activity android:name=".app.EditProduit" />




    </application>

</manifest>